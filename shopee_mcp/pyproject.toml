[project]
name = "shopee-mcp"
version = "0.1.0"
description = "Shopee MCP server for mock shopping and checkout"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.9.0",
  "httpx>=0.28.0",
]

[project.scripts]
shopee-mcp = "main:run"
